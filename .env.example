# Base URL of the Laravel API (fabulousboostbackend).
# Frontend calls are relative paths like /account and /admin/categories,
# which map to the backend's api/* routes — so keep the /api suffix.
VITE_APP_BASE_URL=http://localhost:8000/api
